import { GhostLoaderDirective } from './ghost-loader.directive';

describe('GhostLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new GhostLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
